noob_id: testing-server-asset
assets:
  server:
    type: noob.testing.server_asset
    scope: session
    config: # or load it from a separate yaml
      host: localhost  # or some reference to secure secret
      port: 5432

nodes:
  ping:
    type: noob.testing.Comm
    params:
      msg: pitbull forever
    depends:
      - conn: assets.server
  return:
    type: return
    depends: ping.ping