noob_id: testing-server-asset
assets:
  server:
    type: noob.testing.server_source
    scope: session
    config: # or load it from a separate yaml
      host: localhost  # or some reference to secure secret
      port: 5432

nodes:
  ping:
    type: noob.testing.Comm
    params:
      msg: pitbull forever
    depends:
      - conn: server.conn  # or maybe this should illegal? Not sure about the Connection layer between assets and nodes
  return:
    type: return
    depends: ping.ping