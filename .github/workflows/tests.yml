name: Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-core:
    strategy:
      matrix:
        platform: [ "ubuntu-latest", "macos-latest", "windows-latest" ]
    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v6

      - name: Restore built wheel
        id: wheel-cache
        uses: actions/cache@v4
        with:
          path: packages/noob-core/dist
          key: core-wheel-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('packages/noob-core/src/**', 'packages/noob-core/python/**', 'packages/noob-core/Cargo.toml', 'packages/noob-core/Cargo.lock', 'packages/noob-core/pyproject.toml') }}

      - uses: dtolnay/rust-toolchain@stable
        if: steps.wheel-cache.outputs.cache-hit != 'true'
        id: toolchain
      - uses: Swatinem/rust-cache@v2
        if: steps.wheel-cache.outputs.cache-hit != 'true'
        with:
          workspaces: packages/noob-core
      - name: Build wheel
        if: steps.wheel-cache.outputs.cache-hit != 'true'
        run: pipx run maturin build --release --out dist
        working-directory: packages/noob-core

      - uses: actions/upload-artifact@v5
        with:
          name: core-wheel-${{ matrix.platform }}
          path: packages/noob-core/dist

  test:
    name: pytest
    needs: build-core
    strategy:
      fail-fast: false
      matrix:
        platform: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v3

      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache Dependencies
        id: cache-dependencies
        uses: actions/cache@v6
        with:
          path: .venv
          key: ${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('pylock.toml', 'packages/noob/pyproject.toml') }}

      - name: Install dependencies
        run: |
          python -m pip install pdm coverage
          pdm install

      - uses: actions/download-artifact@v6
        with:
          name: core-wheel-${{ matrix.platform }}
          path: core-dist

      - name: Install repo-built noob-core
        run: |
          pdm run python -m ensurepip
          pdm run python -m pip install --no-index --find-links core-dist noob-core

      - name: Assert core came from the repo wheel
        run: pdm run python -c "import noob_core; print(noob_core.__file__)"

      - name: Run Tests
        run: pdm run test
        timeout-minutes: 5
        env:
          NOOB_LOGS__WIDTH: 140

      - name: Trust Coveralls Homebrew tap (Homebrew 6 requirement)
        if: runner.os == 'macOS'
        run: brew trust coverallsapp/coveralls

      - name: Coveralls Parallel
        uses: coverallsapp/github-action@v2
        continue-on-error: true
        with:
          flag-name: run-${{ join(matrix.*, '-') }}
          parallel: true

  benchmark:
    name: Benchmark
    runs-on: ubuntu-latest
    needs: [build-core]
    steps:
      - uses: actions/checkout@v3
      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          pip install pdm
          pdm install
      - uses: actions/download-artifact@v6
        with:
          name: core-wheel-ubuntu-latest
          path: core-dist
      - name: Install repo-built noob-core
        run: |
          pdm run python -m ensurepip
          pdm run python -m pip install --no-index --find-links core-dist noob-core
      - name: Run the benchmarks
        uses: CodSpeedHQ/action@v4
        timeout-minutes: 5
        with:
          mode: simulation
          run: pdm run benchmark

  finish-coverage:
    needs: test
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - name: Coveralls Finished
        uses: coverallsapp/github-action@v2.3.6
        with:
          parallel-finished: true
